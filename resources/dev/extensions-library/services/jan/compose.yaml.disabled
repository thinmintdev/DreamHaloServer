services:
  jan:
    image: ghcr.io/janhq/jan-server:v0.7.7
    container_name: dream-jan
    restart: unless-stopped
    security_opt:
      - no-new-privileges:true
    environment:
      - LLM_API_URL=http://localhost:${JAN_PORT:-1337}
    volumes:
      - ./data/jan:/app/server/jan:rw
    ports:
      - "${JAN_PORT:-1337}:1337"
    healthcheck:
      test: ["CMD", "wget", "--spider", "-q", "http://localhost:1337/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
    networks:
      - dream-network
    deploy:
      resources:
        limits:
          cpus: '2.0'
          memory: 4G
        reservations:
          cpus: '0.5'
          memory: 1G

networks:
  dream-network:
    external: true
    name: dream-network
